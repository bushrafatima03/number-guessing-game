#  Number Guessing Game

A simple Python number guessing game for beginners.

## How it works

The computer randomly selects a number between 1 and 100.

The player keeps guessing until they find the correct number. The program gives a hint if the guess is too high or too low and counts the number of attempts.

## How to run

Make sure Python is installed, then run:

```bash
python game.py